// Declaración de tipos para @intlify/devtools-types (dependencia opcional)
declare module '@intlify/devtools-types' {
  export interface IntlifyDevToolsEmitter {
    // Tipos vacíos para evitar errores de compilación
  }
  
  export type IntlifyDevToolsHookPayloads = Record<string, any>;
  
  export type IntlifyDevToolsHooks = string;
  
  export interface VueDevToolsEmitter {
    // Tipos vacíos para evitar errores de compilación
  }
}

